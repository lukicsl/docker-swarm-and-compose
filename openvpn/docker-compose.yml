version: '3.4'
services:
    openvpn:
        cap_add:
            - ALL
        restart: always
        image: 'lukics/openvpn:armv7'
        network_mode: host
        ports:
          - 1194:1194/udp        
        volumes:
          - /datavol/openvpn/etc:/etc/openvpn
